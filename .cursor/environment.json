{
  "name": "Jakkash Property Website (Flask + SQLite dev)",
  "install": "sudo apt-get update && sudo apt-get install -y --no-install-recommends python3.12-venv && python3 -m venv venv && ./venv/bin/pip install --upgrade pip && ./venv/bin/pip install -r requirements-local.txt && USE_SQLITE=1 ./venv/bin/python scripts/init_and_seed_local.py",
  "terminals": [
    {
      "name": "flask",
      "command": "USE_SQLITE=1 FLASK_ENV=development FLASK_RUN_HOST=0.0.0.0 FLASK_RUN_PORT=5000 ./venv/bin/python app.py"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 5000
    }
  ]
}
